<div class="post">
  <% if can? :manage, User %>
      <p>Visible only for superadmins and supervisors! </p>
  <% end %>
  <h1>Edit article</h1>
  <%= render 'form' %>
  <div class="przyciski">
    <strong>
      <%= link_to 'Back', articles_path %>
    </strong>
  </div>
</div>

