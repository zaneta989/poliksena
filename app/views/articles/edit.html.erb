<% if cannot? :manage, @article %>
      <div class="notice">
        <p>Visible only for superadmins and supervisors! </p>
      </div>
  <%else%>
      <div class="post">
        <h1>Edit article</h1>
        <%= render 'form' %>
        <div class="przyciski">
          <strong>
            <%= link_to 'Back', articles_path %>
          </strong>
        </div>
      </div>
<% end %>


